import type { NextPage } from "next";
import styled from "styled-components";

const Home: NextPage = () => {
  return (
    <Container>
      <h1>Hello World</h1>
    </Container>
  );
};

const Container = styled.div``;

export default Home;
